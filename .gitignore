.DS_Store
*.log

# demo/setup.sh copies this in from the repo; it is not a second source. The
# hook is not copied, the demo calls the one at the repo root.
demo/homelab/.claude/skills/
!demo/homelab/logs/*.log
